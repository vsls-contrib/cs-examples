services:
  server:
    build:
      context: .
      dockerfile: Server.Dockerfile
    environment:
      GREETING: "Hey there"
    expose:
      - "25250"
  app:
    build: 
      context: .
      dockerfile: App.Dockerfile
      target: base
    volumes:
      - ..:/workspace:cached
    command: /bin/sh -c 'env && sleep infinity'