{
    "build": {
        "dockerfile": "Dockerfile",
        "args": {
            "VARIANT": "focal"
        }
    },
    "onCreateCommand": "echo $USER > USER.txt",
    "remoteUser": "codespacesuser"
}
